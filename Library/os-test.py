import os

print(os.getcwd())  # 当前工作目录

# os.chdir('path')  # 改变当前工作目录

# os.system('mkdir today')  # 创建新目录文件夹

# print(dir(os))  # 函数不带参数时，返回当前范围内的变量、方法和定义的类型列表；带参数时，返回参数的属性、方法列表。如果参数包含方法__dir__()，该方法将被调用。如果参数不包含__dir__()，该方法将最大限度地收集参数信息。

# print(help(os))  # 函数用于查看函数或模块用途的详细说明。
